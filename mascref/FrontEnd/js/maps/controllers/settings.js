'use strict';

/* Transect Controllers */

angular.module('maps.controllers', [])
  .controller('MapsSettingsCtrl', 
    [
      '$scope', 
      '$translate', 
      '$state',
      function (
          $scope, 
          $translate, 
          $state
      ){

        // Init
        
      }
    ]
  );